// Generated by CoffeeScript 1.6.3
var GroupRegistrationView,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

GroupRegistrationView = (function(_super) {
  __extends(GroupRegistrationView, _super);

  function GroupRegistrationView(model) {
    this.model = model;
    this.page = 1;
    this.maxPage = 2;
    GroupRegistrationView.__super__.constructor.call(this, this.model);
  }

  GroupRegistrationView.prototype.render = function() {
    var form, options;
    form = '<div class="form-page" id="page-1">';
    form += UI.getLabel('groupName', 'Group Name');
    form += UI.getTextInput('groupName', this.model.groupName);
    form += UI.nl();
    form += UI.getLabel('branchName', 'Branch');
    options = {
      'ho': 'Head Office',
      'pl': 'Premier League',
      'ch': 'Championship',
      'le': 'League 1',
      'll': 'La Liga',
      'bn': 'Bundesliga'
    };
    form += UI.getSelection('branchName', options, this.model.branchName);
    form += UI.nl();
    form += UI.getLabel('loanOfficer', 'Loan Officer');
    options = {};
    form += UI.getSelection('loanOfficer', options, this.model.loanOfficer);
    form += UI.nl();
    form += UI.getLabel('registrationNumber', 'Registration Number');
    form += UI.getTextInput('registrationNumber', this.model.registrationNumber);
    form += UI.nl();
    form += '</div>';
    form += '<div class="form-page" id="page-2">';
    form += UI.getLabel('meetingLocation', 'Meeting Location');
    form += UI.getTextInput('meetingLocation', this.model.meetingLocation);
    form += UI.nl();
    form += UI.getLabel('meetingDays', 'Meeting Days');
    options = {
      'mo': 'Monday',
      'tu': 'Tuesday',
      'we': 'Wednesday',
      'th': 'Thursday',
      'fr': 'Friday',
      'sa': 'Saturday',
      'su': 'Sunday'
    };
    form += UI.getSelection('meetingDays', options, this.model.meetingDays);
    form += UI.getLabel('meetingFrequency', 'Meeting Frequency');
    options = {
      'w': 'Weekly',
      'f': 'Fortnightly',
      'm': 'Monthly'
    };
    form += UI.getSelection('meetingFrequency', options, this.model.meetingFrequency);
    form += UI.nl();
    form += UI.getSubmit('Submit', 'RegistrationSubmit');
    form += UI.nl();
    form += '</div>';
    form += '<div class="form-nav">';
    form += UI.getBackNext('Back', 'GoBack');
    form += '<span id="page-c">Page ' + this.page + ' out of ' + this.maxPage + '</span>';
    form += UI.getBackNext('Next', 'GoNext');
    form += '</div>';
    return form;
  };

  GroupRegistrationView.prototype.extractData = function() {
    this.model.GroupInformation.groupName = $('#groupName').val();
    this.model.GroupInformation.branchName = $('#branchName :selected').val();
    this.model.GroupInformation.loanOfficer = $('#loanOfficer :selected').val();
    this.model.GroupInformation.registrationNumber = $('#registrationNumber').val();
    this.model.GroupInformation.meetingLocation = $('#meetingLocation').val();
    this.model.GroupInformation.meetingDays = $('#meetingDays :selected').val();
    return this.model.GroupInformation.meetingFrequency = $('#meetingFrequency :selected').val();
  };

  return GroupRegistrationView;

})(FormView);
