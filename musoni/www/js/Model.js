// Generated by CoffeeScript 1.6.3
var Model,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

Model = (function() {
  function Model() {
    this.reachableCallback = __bind(this.reachableCallback, this);
  }

  Model.prototype.submit = function() {};

  Model.prototype.reachableCallback = function(reachability) {
    var storeObj;
    if (reachability === NetworkStatus.NOT_REACHABLE) {
      storeObj = this.getStoreObject();
      storeObj.formData = new Encryptor().encrypt(storeObj.formData);
      return this.storeLocally(storeObj);
    } else {

    }
  };

  Model.prototype.getStoreObject = function() {
    throw "Not implemented (abstract class)";
  };

  Model.prototype.getStoreObject = function() {
    throw "Not implemented (abstract class)";
  };

  Model.prototype.storeLocally = function() {
    throw "Not implemented (abstract class)";
  };

  return Model;

})();
