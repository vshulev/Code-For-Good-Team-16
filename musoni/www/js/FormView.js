// Generated by CoffeeScript 1.6.3
var FormView;

FormView = (function() {
  function FormView(model) {
    var _this = this;
    this.model = model;
    window.RegistrationSubmit = function() {
      return _this.submission();
    };
    window.GoNext = function() {
      if (_this.page < _this.maxPage) {
        $('.form-page').hide();
        _this.page += 1;
        $('#page-' + _this.page).show();
      }
      if (_this.page === _this.maxPage) {
        $('#Next').attr('disabled', 'disabled');
      }
      $('#Back').removeAttr('disabled');
      return $('#page-c').html('Page ' + _this.page + ' out of ' + _this.maxPage);
    };
    window.GoBack = function() {
      if (_this.page > 1) {
        $('.form-page').hide();
        _this.page -= 1;
        $('#page-' + _this.page).show();
      }
      if (_this.page === 1) {
        $('#Back').attr('disabled', 'disabled');
      }
      $('#Next').removeAttr('disabled');
      return $('#page-c').html('Page ' + _this.page + ' out of ' + _this.maxPage);
    };
  }

  FormView.prototype.submission = function() {
    this.extractData();
    return this.model.submit();
  };

  FormView.prototype.render = function() {
    throw "Not implemented (abstract class)";
  };

  FormView.prototype.extractData = function() {
    throw "Not implemented (abstract class)";
  };

  return FormView;

})();
